# ssh
# can run ssh-agen but that asks for an x11 password prompt
# alternative is keychain
# source $HOME/.config/shell/load_function.sh
# load_function ask

# ask "Start Hyprland?" Y && $HOME/.config/hypr/scripts/start.sh
