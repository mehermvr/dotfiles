-- this follows much of the style of LazyVim/LazyVim by folke.
-- but since I don't need to support arbitrary users and arbitrary plugin specs, but i do want to make specific changes that basically need a ground up write anyways, just doing it
-- bootstrap lazy.nvim and plugins
require("start")
